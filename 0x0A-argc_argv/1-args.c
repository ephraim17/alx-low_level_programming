#include <stdio.h>

/*
 * main - print the number of arguments passed into it
 * @argc: argument count
 * Return: (0) Success.
 */

int main(int argc, char **argv)
{
	printf("%d\n", argc);
	(void)argv;
	return (0);
}
